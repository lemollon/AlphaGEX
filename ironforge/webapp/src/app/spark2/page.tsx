'use client'

import BotDashboard from '@/components/BotDashboard'

/* SPARK2 — SPARK's full v2 strategy + sizing on the second live account
 * (ex-KINDLE). Own tables/config/creds; born paused. */
export default function Spark2Page() {
  return <BotDashboard bot="spark2" accent="blue" />
}
